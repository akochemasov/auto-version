{
  "name": "auto-version",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "ls -la",
    "version-minor": "git pull origin master && bump --minor --tag --push --all"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "husky": "^1.3.1",
    "version-bump-prompt": "^4.3.0"
  },
  "husky": {
    "hooks": {}
  }
}
